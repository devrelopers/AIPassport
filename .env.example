# ---------------------------------------------------------------------------
# AIPassport Broker Configuration
# ---------------------------------------------------------------------------

# AI Provider API Keys
# These are stored server-side only and never exposed to third-party apps.
# At least one provider key is required for the proxy to work.
#
# AIPassport accepts common alternative names for each provider key —
# use whichever name you already have set in your environment.
# The first matching variable wins.
#
# Anthropic (any of these work):
#   ANTHROPIC_API_KEY, ANTHROPIC_KEY, CLAUDE_API_KEY, CLAUDE_KEY, CLAUDE_SECRET_KEY
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# OpenAI (any of these work):
#   OPENAI_API_KEY, OPENAI_KEY, OPEN_AI_API_KEY, OPEN_AI_KEY
OPENAI_API_KEY=sk-your-openai-key-here

# Google (any of these work):
#   GOOGLE_API_KEY, GOOGLE_AI_API_KEY, GEMINI_API_KEY, GEMINI_KEY
# GOOGLE_API_KEY=your-google-ai-key-here

# Server
PORT=3001

# Token Settings
# How long delegated JWT tokens remain valid, in seconds.
TOKEN_TTL_SECONDS=3600

# JWT Signing
# HMAC secret for signing delegated tokens. If not set, a random key is
# generated on startup (tokens will not survive restarts).
# For production, set this to a long random string.
# JWT_SECRET=change-me-to-a-long-random-string
